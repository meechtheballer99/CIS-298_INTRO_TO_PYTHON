# Local Setup Notes

Generated by `project-setup-helper`.

Generated at: 20260618_235939

## Activate the virtual environment

```bash
source .venv/bin/activate
```

## Project style

Selected style:

```text
requirements
```

## Virtual environment Python

The virtual environment Python should be located at:

```text
/Users/meechtheballer99/Library/CloudStorage/OneDrive-Personal/Documents/GitHub/CIS-298_INTRO_TO_PYTHON/python_project_setup_template/python_project_setup_template-packaged-version/.venv/bin/python
```

Verify it with:

```bash
/Users/meechtheballer99/Library/CloudStorage/OneDrive-Personal/Documents/GitHub/CIS-298_INTRO_TO_PYTHON/python_project_setup_template/python_project_setup_template-packaged-version/.venv/bin/python --version
```

## macOS Apple Silicon: Homebrew Python

Apple's system Python usually lives here:

```text
/usr/bin/python3
```

For development on Apple Silicon Macs, it is usually better to install
Python using Homebrew:

```bash
brew install python
```

Homebrew's Python usually lives here:

```text
/opt/homebrew/bin/python3
```

Verify which Python your shell finds first:

```bash
which -a python3
python3 --version
```

If `/usr/bin/python3` appears before `/opt/homebrew/bin/python3`, add
Homebrew to the front of your PATH:

```bash
echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.zprofile
source ~/.zprofile
```

Then verify again:

```bash
which python3
python3 --version
```
